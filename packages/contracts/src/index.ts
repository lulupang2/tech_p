/**
 * Marker type proving the initial workspace package boundary.
 * Product contracts are introduced by CON-001.
 */
export interface WorkspaceMarker {
  readonly package: '@techpulse/contracts';
}
