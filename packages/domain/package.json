{
  "name": "@techpulse/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "format": "pnpm --dir ../.. exec prettier --check packages/domain",
    "lint": "eslint .",
    "static": "pnpm run typecheck && pnpm run lint && pnpm run format",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
